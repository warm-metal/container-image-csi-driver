runtime:
  engine: cri-o
  socketPath: /var/run/crio/crio.sock
kubeletRoot: /var/lib/kubelet
snapshotRoot: /var/lib/containers/storage
crioRuntimeRoot: /run/containers/storage
crioMountProgram: /usr/local/bin/fuse-overlayfs
logLevel: 4
enableDaemonImageCredentialCache:
pullImageSecretForDaemonset:

# Image credential provider settings
# NOTE: CRI-O test environment does not have credential providers configured
# Standard Kubernetes secrets (imagePullSecrets) are used instead
imageCredentialProvider:
  # Disable credential provider for CRI-O (not available in test environment)
  enabled: false
  # Path to the credential provider plugin config file
  configPath: "/etc/kubernetes/image-credential-providers/config.json"
  # Path to the directory containing credential provider plugin binaries
  binDir: "/etc/kubernetes/image-credential-providers"

csiPlugin:
  resources: {}
  metricsPort: 8080
  image:
    tag: ""
    repository: docker.io/warmmetal/container-image-csi-driver
    pullPolicy: IfNotPresent
  livenessProbe:
    httpGet:
      path: /healthz
      port: metrics
    failureThreshold: 5
    initialDelaySeconds: 10
    timeoutSeconds: 10
    periodSeconds: 60
csiLivenessProbe:
  resources: {}
  image:
    repository: registry.k8s.io/sig-storage/livenessprobe
    tag: v2.10.0
    pullPolicy: IfNotPresent
csiNodeDriverRegistrar:
  resources: {}
  image:
    repository: registry.k8s.io/sig-storage/csi-node-driver-registrar
    tag: v2.8.0
    pullPolicy: IfNotPresent
csiExternalProvisioner:
  resources: {}
  image:
    repository: registry.k8s.io/sig-storage/csi-provisioner
    tag: v3.5.0
    pullPolicy: IfNotPresent
tolerations: {}
affinity: {}
nodeSelector: {}

fullnameOverride: ""

podMonitor:
  enabled: true
  interval: 30s
  timeout: 10s
